import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static var primaryColor = const Color(0xff0D7CF1);
  static const secondaryColor = const Color(0xff0D7CF1);
  static const lightBackgroundColor = ();
  static const darkBackgroundColor = ();
  // Light theme colors
  static const lightPrimary = Color(0xFF6200EE);
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF000000);
  static const lightappbar = Colors.blueAccent;
  static const lightaddbtn = Colors.black;

  // Dark theme colors
  static const darkPrimary = Color(0xFFBB86FC);
  static const darkBackground = Color(0xFF121212);
  static const darkText = Color(0xFFFFFFFF);
  static const darkappbar = Colors.black;
  static const darkaddbtn = Colors.white;
}
